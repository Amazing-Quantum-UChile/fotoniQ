

## Working with poetry
To add a module, run 
```{code}
poetry add module__name
```
To run a script without entering the environment, run 
````
poetry run python mon_script.py
````
but to activate the environment in the shell, run 
```poetry shell```.